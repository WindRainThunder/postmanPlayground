$kind: http-request
name: ShouldNotFindUser
url: "{{base_url}}/api/users/{{userId}}"
method: GET
headers:
  x-api-key: "{{vault:x-api-key}}"
scripts:
  - type: afterResponse
    code: |
      const json = pm.response.json();

      pm.test("Status 404", () => {
          pm.response.to.have.status(404);
      });

      pm.test("Empyt body", () => {
          pm.response.to.have.body("{}");
      });
    language: text/javascript
order: 3596418218564139
